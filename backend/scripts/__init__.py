# FASE_KPI_CONSISTENCY: marcar `scripts/` como paquete importable desde el backend
# (necesario para que `from scripts.validate_kpi_grain_consistency import ...` funcione
# desde routers FastAPI sin depender de namespace packages).
